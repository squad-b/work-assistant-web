class Book {
  constructor(id, title, subTitle, thumbnail, categories, bookRental) {
    this.id = id;
    this.title = title;
    this.subTitle = subTitle
    this.thumbnail = thumbnail;
    this.categories = categories;
    this.bookRental = bookRental;
  }
}

export default Book;
